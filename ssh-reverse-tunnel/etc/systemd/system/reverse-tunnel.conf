# Reverse SSH Tunnel Configuration
# Copy this file to reverse-tunnel.conf and edit values
# reverse-tunnel.conf is in .gitignore - never commit it

# SSH key for tunnel authentication
TUNNEL_KEY=/home/zoran/.ssh/id_ed25519

# Local user to run the tunnel as
TUNNEL_USER=zoran

# Remote Google VM
REMOTE_USER=zorand
REMOTE_HOST=104.154.60.182
REMOTE_BIND_IP=10.240.0.2

# Port mapping: REMOTE_PORT -> localhost:LOCAL_PORT
REMOTE_PORT=15000
LOCAL_PORT=22
